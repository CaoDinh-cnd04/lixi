# URL Backend (chỉ dùng lúc build). Để trống nếu chạy local và dùng host:5000 (LAN).
# - Chạy frontend local + backend local: http://localhost:5000
# - Deploy frontend, backend trên cloud: https://your-backend.railway.app
# - Frontend GitHub Pages + Backend máy tính (ngrok): để trống, cấu hình URL trong Admin sau khi deploy
# VITE_API_URL=http://localhost:5000
